# Core web framework
streamlit>=1.38.0

# Data handling
pandas>=2.2.0
numpy>=1.26.0

# Data source
yfinance>=0.2.40

# Plotting
plotly>=5.22.0

# Used for date parsing (yfinance news dates)
python-dateutil>=2.8.2

# Timezone support (you are using pytz in app.py)
pytz>=2024.1

# Optional but recommended for better performance & stability
# (especially when deploying to Streamlit Community Cloud)
pyarrow>=15.0      # faster parquet / arrow support in pandas
tornado>=6.4       # helps with websocket stability on cloud

# Only needed if you want to export static images on server/cloud
# (you are already using kaleido in the code)
kaleido==0.2.1
lxml>=5.3.0
